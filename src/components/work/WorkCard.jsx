import { useReducedMotion, motion } from 'framer-motion'
import ProjectGallery from './ProjectGallery'
export default function WorkCard({ project }) { const reduceMotion = useReducedMotion(); return <motion.article initial={reduceMotion ? false : { opacity: 0, y: 20 }} whileInView={reduceMotion ? {} : { opacity: 1, y: 0 }} transition={{ duration: .4, ease: 'easeOut' }} viewport={{ once: true, amount: .15 }} className="rounded-3xl border border-border bg-background p-6 sm:p-8"><ProjectGallery images={project.images} brand={project.brand} /><div className="mt-8"><p className="text-xs font-bold uppercase tracking-[.18em] text-accent">{project.category}</p><h3 className="mt-2 text-2xl font-semibold">{project.brand}</h3><p className="mt-3 max-w-lg leading-7 text-secondary">{project.bio}</p><ul className="mt-5 flex flex-wrap gap-2">{project.services.map(service => <li key={service} className="rounded-full border border-border px-3 py-1 text-xs text-secondary">{service}</li>)}</ul></div></motion.article> }

